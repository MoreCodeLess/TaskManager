{
  "name": "appsmoviles",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "clean:dev": "rimraf ./dist",
    "clean:prod": "rimraf ./dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel",
    "dev": "npm run clean:dev && parcel",
    "prod": "npm run clean:prod && parcel build",
    "build": "parcel build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.3",
    "parcel": "^2.9.3",
    "sass": "^1.66.1"
  }
}